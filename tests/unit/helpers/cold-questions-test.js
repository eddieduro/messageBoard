import { coldQuestions } from 'message-board/helpers/cold-questions';
import { module, test } from 'qunit';

module('Unit | Helper | cold questions');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = coldQuestions([42]);
  assert.ok(result);
});
